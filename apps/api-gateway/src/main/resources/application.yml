server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka/

spring:
  application:
    name: api-gateway

logging:
  level:
    org.springframework.cloud.gateway: TRACE
